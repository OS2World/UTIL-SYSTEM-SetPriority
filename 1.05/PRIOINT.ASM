.MODEL TPASCAL

.286

.CODE

PUBLIC SetPriority
SetPriority PROC FAR
	PUSH BP
	MOV BP, SP

.386
	MOV EAX, DWORD PTR SS:[BP+0ah]
	MOV EDX, DWORD PTR SS:[BP+06h]
	INT 45h

.286
	LEAVE
	RET 8
SetPriority ENDP

PUBLIC GetPriority

GetPriority PROC FAR
	PUSH BP
	MOV BP,SP

.386
	MOV EAX, 00000042h
	INT 45h

.286
	PUSH ES
	PUSH DI

	LES DI, DWORD PTR SS:[BP+0ah]
.386
	MOV DWORD PTR ES:[DI], EAX

.286
	LES DI, DWORD PTR SS:[BP+06h]
.386
	MOV DWORD PTR ES:[DI], EDX

	POP DI
	POP ES

.286
	LEAVE
	RET 8
GetPriority ENDP

END